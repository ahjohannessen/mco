mco.defaults {
  name = <no name>
  repo = isolated
  source = "./source"
  target = "./target"

  media = [mco.io.ArchiveMedia, mco.io.FolderMedia]
  classifiers = [mco.io.SimpleClassifier]

  manual {
    addition = true
    installation = false
  }
}
